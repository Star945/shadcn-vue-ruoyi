# 交付状态

## 已完成

- 真实若依登录、`/getInfo`、菜单、权限与主要业务接口已经接通。
- 核心系统页已经改成独立业务页，覆盖用户、角色、菜单、部门、岗位、字典、参数、公告。
- 监控页已接入在线用户、登录日志、操作日志、定时任务、服务监控、缓存监控。
- 工具页已覆盖代码生成、 Swagger、Druid、表单构建，并完成基础权限收口。
- 主题系统支持明暗模式、主题预设、自定义主题色和圆角半径。
- 标签页支持刷新、关闭、批量关闭，并增加右键上下文菜单。
- 兼容若依常见旧路径、隐藏页路径和后端 `component` 字符串映射。
- 浏览器入口已统一为中文站点标题，并替换为项目品牌 favicon。

## 当前仍与原版若依存在差异

- `tool/build` 是增强版本地表单设计工作台，不是原版若依的完全同构实现。
- `Druid` 和 `Swagger` 仍然是嵌入式页面，而不是深度重做后的业务页。
- 部分视觉表现做了现代化调整，不是原版 Element UI 风格复刻。
- 仓库中保留了 `_references` 和 `_scaffold` 作为历史参考，但它们不参与运行。

## 建议验收路径

1. 登录与注册：验证验证码、登录态恢复、退出登录。
2. 动态菜单：验证后端菜单驱动、旧路径直达、隐藏业务页跳转和路由级权限。
3. 系统管理：验证用户、角色、菜单、部门、岗位、字典、参数、公告的查询与 CRUD。
4. 监控中心：验证登录日志、操作日志、在线用户、定时任务、缓存监控、服务监控。
5. 主题系统：验证主题色、深浅模式、圆角半径、标签页操作与右键菜单。
6. 工具模块：验证代码生成、Swagger、Druid、表单构建页面访问与权限边界。

## 后续建议

- 做一轮真实后端联调回归，重点看按钮级权限和少量边角字段。
- 如果要正式交付，可在确认无保留价值后移除 `_references`、`_scaffold` 和根目录异常产物 `-g`。
- 如果要继续向“完全 1:1 若依复刻”推进，优先补 `tool/build` 与嵌入页的行为差异。
